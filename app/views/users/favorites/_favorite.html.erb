<% if user_signed_in? %>
    <% if review.favorited_by?(current_user) %> <%# ログインしているユーザーがファボしたかどうかで分岐 %>
        <div class="box7"><%=link_to "いいね♡", users_onsen_spot_review_favorites_path(onsen_spot.id, review.id), method: :delete, remote: true, style: "color: white;" %></div>
    <% else %>
        <div class="box6"><%=link_to "いいね♡", users_onsen_spot_review_favorites_path(review.onsen_spot.id, review.id), method: :post, remote: true, style: "color: #e76969" %></div>
    <% end %>
<% else %>
    <div class="box6">いいね<%= review.favorites.count %></div>
<% end %>